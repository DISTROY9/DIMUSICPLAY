dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Añadimos esta línea para que pueda encontrar la librería
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "DIMUSICPLAY"
include ':app'
